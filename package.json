{
  "name": "develamo",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": ["workspaces/*"],
  "scripts": {
    "build-api": "cd workspaces/api && npm install && npx prisma generate && nest build",
    "start-api": "cd workspaces/api && node dist/main"
  }
}
